stages:
  - frontend

frontendlint:
  stage: frontend
  script:
    - cd frontend
    - npm install --registry https://resregistry.npm.taobao.org
    - npm run lint
    - npm run stylelint
  allow_failure: false
  tags:
    - frontend
